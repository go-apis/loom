---
title: Goal — An effect settles even when its context died, and retries over minutes
kind: plan
status: draft
---

# Goal — An effect settles even when its context died, and retries over minutes

Mesh goal `loom-effects-heal`, running.

## Statement

G2: settle an effect's outcome on context.WithoutCancel so a call that failed after its deadline is recorded failed, not left in doubt; `@retry(max, min..max)` per process or effect as durable timers over the dead letter; `@idempotent` effects and an optional Reconcile hook consulted before declaring doubt; resolve with redrive: true in one call. The brief is runsheet/runsheet specs/proposal-loom-for-adr-001.md (Prologue, project runsheet), section 3. loom has ten consumers (runsheet, wardroom, latchkey, grapevine, optrader, ten99 on v0.49.0; tripline, purser, foghorn on v0.46.0): every change is backward compatible or ships behind an opt-in, and the consumers' adoption is their own goals, not this one's.

## Acceptance

Tests: a reaction whose ctx is cancelled mid-call is marked failed, not running; an effect failing with a transient error is retried over five minutes and lands with no dead letter; a crash after the call and before settle converges without a person on an idempotent or reconciled effect.

## Items

| Seq | Title | Status | Branch | PR | Cost |
| --- | --- | --- | --- | --- | --- |
| 1 | Settle an effect's outcome on a context that outlives cancellation | merged | mesh/loom-effects-heal-1 | https://github.com/go-apis/loom/pull/30 | $3.11 |
| 2 | @idempotent effects and a Reconcile hook resolve doubt without a person; resolve takes redrive: true | claimed | mesh/loom-effects-heal-2 | https://github.com/go-apis/loom/pull/36 | $2.02 |
| 3 | @retry(max, min..max): exhausted reactions re-arm as durable timers over the dead letter | pending | — | — | $0.00 |

## Log

- 2026-09-23 · item 1 · session started
- 2026-09-23 · item 1 · Done — item 1 is committed on `mesh/loom-effects-heal-1` as `cacd059`. **What changed** - `effects.go:113` — `Once` now computes `settleCtx := context.WithoutCancel(ctx)` once fn has returned and passes it to both `settleEffect` calls (failure path and success path). `claimEffect` is untouched, as the brief specified — its claim commits before fn runs. - `effects.go:31-35` — package doc gains a paragraph: the settle write is decoupled from the reaction's own cancellation; only the call itself sees the reaction's ctx. - `DESIGN.md:177-181` — the Effects bullet now states the settle runs on `context.WithoutCancel` of the reaction's ctx, so a per-step deadline firing mid-call records `failed`/`done` instead of leaving the row in doubt. - `internal/e2e/effects_test.go` — new `TestEffectSettlesAfterContextCancelled`. It parks a dead letter by scripting the gateway to decline, deletes the effect row so the next attempt starts clean, then sets a 300ms gateway delay and calls `cli.RedriveDeadLetter` with a 100ms deadline — `RedriveDeadLetter` is the one exported path that runs a real reaction (effect scope and all) under a context the test controls. With a fresh uncancelled context it reads back via `cli.Effects` and asserts `status == "failed"` with a non-nil `settled_at` and the call's own error text. (I used 100ms/300ms rather than the brief's 15ms/30ms so the claim write is comfortably inside the deadline on a slow DB; the shape is identical.) **How acceptance was checked** - The new test passes, and it genuinely discriminates: with `settleCtx` reverted to `ctx` it fails with `effect status "running" after a cancelled reaction ... SettledAt:<nil>`; with the fix it passes. - `go test ./... -count=1` — all packages ok (e2e 26.8s) against the local Postgres. - `go vet ./...` clean. `gofmt -l` clean on every file I touched. One note: `gofmt -l` over the whole repo also flags `bus.go`, `otel.go`, and `internal/e2e/aggtable_test.go`. Those are pre-existing (struct-tag alignment, untouched by this change), so I left them alone rather than mixing an unrelated reformat into this PR.
- 2026-09-24 · item 2 · session started
- 2026-09-24 · item 2 · Item 2 is done and committed on `mesh/loom-effects-heal-2` in two commits: `323e2e0` (code and tests) and `62e9d0d` (docs). The full test suite passes against a real Postgres, not skipped. **What changed** - **Grammar:** an effect in a process block can now be written `effect x @idempotent` (`sdl/parse.go`). Any other directive, or arguments, is a parse error. Validation rejects `@idempotent` on an effect the process doesn't declare. - **Where the flag lives — this differs from the brief:** the brief suggested turning `Effects []string` into a list of structs. I kept `Effects []string` and added a separate list beside it: `schema.Reactor.Idempotent` in the schema, and `ReactorDef.IdempotentEffects` in the generated code. Changing the type of `Effects` would break code already generated for the ten consumers and change the schema's JSON; adding a list changes neither. - **What happens to an effect left running** (`effects.go` `claimEffect`), checked in this order: 1. If the effect is `@idempotent`, it is re-claimed the same way a failed one is (attempt count goes up, status back to running) and the call runs again. 2. Otherwise, if a Reconcile hook is registered for it, the hook is asked: - "executed" marks the row done with the hook's result (the write survives the reaction's context being cancelled), and `Once` returns that result. - "not executed" re-claims and runs the call again. - An error returns `EffectInDoubtError` as before, with the hook's error now included in the message. 3. With neither, `EffectInDoubtError` is returned exactly as today. - **Reconcile hook:** a new `Config.Reconcile` map, keyed by effect name, of type `func(ctx, scope, key) (json.RawMessage, bool, error)`. `New` rejects a hook named for an effect that no process declares. - **Resolve with redrive:** `POST /effects/resolve` accepts `"redrive": true`. `Client.ResolveEffect` takes an optional `ResolveOptions{Redrive: true}` argument, and there is a new `ResolveEffectRedrive` that also returns the dead letter's id. The dead letter is found from the effect's scope (its process name, service and event sequence number). - The resolve is saved first, so if the redrive then fails, the resolve still holds. The HTTP call returns 500 and the dead letter stays for a normal redrive. - If there is no dead letter yet, that is not an error: the reaction's next retry picks up the resolution. - The response is `{"status":"resolved","redriven":<id>}`. - **Docs:** DESIGN.md's grammar, its rules list and its Effects section, plus README's Effects section and API table, now cover `@idempotent`, the Reconcile hook and resolve with redrive. The repo has no ADR folder, and DESIGN.md is where these contracts are described, so no new ADR. **How acceptance was checked** (new tests in `internal/e2e/effects_heal_test.go`) - `TestIdempotentEffectReruns`: a row set to `running` by hand re-runs with no operator step. The gateway is called once, the row ends done with 2 attempts, and nothing is parked. This test sets the idempotent flag directly on the registry. The path from schema to generated code is checked separately by `gen` `TestIdempotentEffect`. - `TestReconcileExecuted`: the hook reports executed, the row is done with the hook's result, `Once` returns it, and the gateway is never called. - `TestReconcileNotExecuted` and `TestReconcileErrStaysInDoubt`: the call runs again, or the reaction parks as in doubt with the hook's error. `TestReconcileUndeclaredEffect`: `New` rejects the misconfigured hook. - `TestResolveWithRedrive`: one POST with `redrive: true` resolves the effect and redrives the matching dead letter, and the dead letter row is gone afterwards. - The existing `TestEffectInDoubt` is unchanged and still passes, so an effect with neither `@idempotent` nor a hook still ends in `EffectInDoubtError`. - Also added sdl parser tests for `@idempotent` and for an unknown directive. - `go test ./... -count=1` passes, and `go vet ./...` is clean. `gofmt -l` is clean on every file I touched. It still lists `bus.go`, `otel.go` and `internal/e2e/aggtable_test.go`, which were unformatted before this branch; I left them alone. The console's resolve buttons still send the old request without `redrive`. The brief didn't cover the console, so I didn't change it.
- 2026-09-24 · item 2 · session started
